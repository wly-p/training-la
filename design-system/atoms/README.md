# L1 原子

不可再分。只收 `String` / `Int` / `Bool` / closure，**不得認識任何 domain 型別**。
住 `Packages/DesignSystem/Sources/DesignSystem/Atoms/`，一律 `TL` 前綴。

每個元件一個目錄，含 `X.spec.md` 與 `X.preview.html`（實作檔在 package 裡，由 spec 第 1 節指出）。

原子沒有第 2 節的 slots、也沒有第 11 節組成；其餘各節都要填。
