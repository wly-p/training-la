# <元件名>

> 規格樣板。十二節，標 ★ 的可機器驗。
> L1 原子通常沒有第 2 節的 slots、也沒有第 11 節；其餘各節都要填。
> **不適用的項目寫 `N/A` 並附理由，不要刪掉標題** —— 省略正是狀態遺失的方式。

## 1 身分

| | |
|---|---|
| 層級 | L1 原子 / L2 分子 / L3 有機體 |
| 職責 | 一句話 |
| 原型 id | `4c` / `13a` …（沿用設計文件的 id） |
| 獨立使用 | 是 / **否 —— 必須置於 `TLxxx` 之內** |
| 容器責任 | 哪些事情不歸這個元件管（間距、位置、按下態…） |
| 實作 | `Packages/…/X.swift` |
| Preview | `design-system/…/X.preview.html` |
| Preview CSS | `components.preview.css` §X |

> 「獨立使用」與「容器責任」是給組畫面的人的護欄。L1 原子幾乎都**自己不完整**——
> badge 要放在列裡、chevron 要放在可進入的列裡。不寫清楚的話，組畫面的人會直接把它
> 擺在畫面上，然後間距全錯。這兩格會進 `components.json`。

## 2 介面 ★

**Props**

| 名稱 | 型別 | 值域 | 必填 | 預設 | 說明 |
|---|---|---|---|---|---|

> **值域比型別重要。** `style: TLTagStyle` 這一行的資訊量是零 ——
> 要寫出那 6 種 style 分別叫什麼（`accent / sage / neutral / outline / danger / muted`）。
> 沒有值域，組畫面的人只能猜 case 名稱，猜錯就硬編一個顏色上去。
> `Bool` 自明可留空；數字要寫單位與是否只吃 token 值；closure 要寫 signature。

**Slots** — 可插入的子內容（SwiftUI 的 `@ViewBuilder`）
**事件** — `onTap` / `onChange` …

> 這節決定「應用層只能排列堆疊」是否可能。props 不夠，應用層就會被迫硬編。
> 也是判斷「元件庫夠不夠用」的唯一依據。

## 3 變體 variants

互斥的樣式選擇。**要寫出變體之間哪些狀態不通用**（例：`text` 變體沒有 pressed 底色）。

| 變體 | 說明 |
|---|---|

## 4 狀態 states ★

<!-- states: default -->
<!-- themes: light, dark -->

上面兩行是**機器讀的**，分工要分清楚：

- `states` = **這個元件自身**會有的樣子（default／pressed／disabled／selected／loading…）。
  `preview.html` 必須每個都有對應的 `data-state="…"`。
- `themes` = **維度**，不是狀態。由 preview 的 `data-theme="…"` 承接（並排雙欄或切換皆可）。
  **不要把 light／dark 寫進 `states`** —— 那會讓機器檢查通過得沒有意義，
  而且到了有真狀態的 L2／L3，會分不出哪些是真狀態、哪些是維度。機器檢查會擋。

下面的表格是給人讀的，逐項回答，不適用寫 `N/A` ＋ 理由。

| 類別 | 狀態 | 這個元件 |
|---|---|---|
| 互動 | default / pressed / disabled / focused | |
| 選取 | selected / unselected / indeterminate | |
| 資料 | empty / loading / error | |
| 內容極值 | 最短 / 最長 / 溢出 / 數字最大位數 | |
| 主題 | light / dark | |
| 語言 | 中文 / 英文（字級 ×1.08） | |

## 5 度量

全部寫 token 名，不寫數字。

| | |
|---|---|
| 尺寸 | |
| 內距 | |
| 最小寬度 | 被壓到多窄還成立 |
| 壓縮行為 | 空間不夠時誰先讓（截斷／換行／等比縮） |

> 最小寬度與壓縮行為講的是**元件的佔位**，跟第 7 節的「多語系文字寬度」不同。
> 排版的人要知道「兩個並排放得下嗎」「中文換英文會不會擠掉旁邊的東西」。

## 6 配色 ★

只列語意 token，不出現任何 hex。

## 7 文字行為

溢出處理 / 多語系寬度 / Dynamic Type 行為（現在不實作，位置先寫）。

## 8 動態

轉場 / 動畫時長與曲線 / 觸覺回饋。全部寫 `motion.*` token。

## 9 無障礙

accessibility label / trait、最小觸控 44pt、`accessibilityIdentifier` 慣例。

## 10 用法與禁用法

**用它**：…

**易混淆**：`TLxxx`（它是「…」）—— 這個是「…」

> 固定寫成一行對照，並且**兩邊的規格都要寫**。會搞混的時候，人看的是其中一份，不是兩份。

**不要用它**：
- 禁止 —— 為什麼

> 每條都要有「為什麼」那半句，否則讀的人會照做但不知道邊界，遇到相似情況還是會猜錯。
> **3–4 條是上限**：超過就代表這個元件職責混了，那時該拆元件，不是加禁令。

## 11 組成 ★

（L2 / L3 才有）由哪些下層元件組成。

## 12 變更紀錄

偏離設計稿時記這裡，含原因。
