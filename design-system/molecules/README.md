# L2 分子

由原子組成，仍與 domain 無關。住 `Packages/DesignSystem/Sources/DesignSystem/Molecules/`，`TL` 前綴。

每個元件一個目錄，含 `X.spec.md` 與 `X.preview.html`（實作檔在 package 裡，由 spec 第 1 節指出）。
規格第 11 節要列出它由哪些 L1 組成——`make lint` 會檢查那些原子真的在庫裡。
