# 第五輪審閱 · 四題回覆

> 回覆 `HANDOFF.md` 第五輪
> 日期：2026-08-30

每項都標了性質：**〔要改〕**有問題、**〔決定〕**你問我拍板、**〔建議〕**可不採納。

---

## Q1 · `components.preview.css` 的分段粒度

### 〔要改〕例外不合理，但要改的是骨架的歸屬，不是補三個段落

你的理由是「`TLGroup`／`TLDivider`／`TLDividedVStack` 的視覺表述**就是**骨架的 `.group`／`.row`」。

我同意那個觀察，但結論剛好相反：**如果 `.group` 就是 `TLGroup` 的視覺表述，那它不是骨架，是放在錯檔案裡的元件鏡像。**

分家的判準是「這段 CSS 描述的是產品的一部分嗎」。`.screen`（手機外框）、`.kicker`、`.note`、`.pane`、`.themes`、`code` 都不是產品，它們是展示用的紙張。但 `.group` 有 `surface-raised` 底 ＋ `radius-container` ＋ `overflow:hidden`，`.row` 有 `size-row` ＋ `rowInset` ＋ `::after` 分隔線 —— **那是三個元件的規格，逐項對得上。**

留著這個例外的代價：檢查 16 會有一個**永久的白名單**，而白名單會被引用（「`TLGroup` 都可以不寫，我這個也差不多」）。四件套的規則變成「除了三個以外」。

**建議的改法**：

```
preview.css              .screen .kicker .note .pane .themes code   ← 真骨架
components.preview.css   .tl-group / .tl-row / .tl-divider          ← 移進來
```

`.row` 由多個列元件共用，所以它需要一個處置。兩個選項，我傾向前者：

- **(a)** `.tl-row` 自成一段，段落註解寫明「`TLGroup` 的子列共用的底；各列元件的段落只寫差異」。檢查 16 的規則放寬成「一個 `.tl-` 段落可對應多個元件，但要在註解裡列出是哪些」
- **(b)** 每個列元件各自完整寫一遍。乾淨但重複，而且重複的東西會漂移

順帶：那三個元件的規格第 1 節現在應該也沒有 `Preview CSS` 那一列 —— 改完要補上，否則四件套在它們身上是三件。

---

## Q2 · 19 份規格的狀態有沒有漏

### 〔要改，但改的是呈現方式〕逐份讀我抓不出來

老實說：讀 19 份規格的第 4 節，我只會看到「每一項都有回答」，而你擔心的是「回答成 N/A 的那些其實有」。**那種漏靠順序閱讀抓不到，要靠對照。**

**我需要的是一張表**：19 個元件 × 標準狀態矩陣的 6 類，每格是「有／N/A／未回答」。

```
                    互動          選取        資料      極值   主題   語言
TLChevron           default        N/A        N/A       N/A    ✓✓    N/A
TLListRow           def/pressed    ⚠N/A       N/A        ✓     ✓✓     ✓
TLSettingsToggleRow def/pressed    on/off     N/A        ✓     ✓✓     ✓
...
```

這張表從 `components.json` 就能生成，放在 `index.html` 或單獨一頁。**掃一列比讀一份規格快十倍，而且「同一類狀態在相似元件上答案不同」會自己跳出來** —— 例如上面那個 `TLListRow` 的選取寫 N/A，但它旁邊的 toggle row 有 on/off，兩個都是列，這種落差在表上一眼看到，在 19 份文件裡看不到。

這也是「窮舉狀態」這條規則第一次真的能被檢驗：現在窮舉的是**單一元件內**，表格讓它變成**跨元件**。

給我表，我一次回。

---

## Q3 · 易混淆的六組

### 〔決定〕六組都配得對，但**漏了三組**

現有六組沒有配錯的，判準也都寫成了可判斷的形式（「看到圓章就是 `TLListRow`」這種）。

漏的三組，都是我實際會選錯的：

**1 · `TLToggle` ↔ `TLSegmentedControl`**
兩個都是「就地改一個設定、不進下一頁」。判準：**二元開關用 Toggle、2–3 個具名選項用 SegmentedControl**。設定頁裡「重量單位 kg|lb」用了分段控制而不是開關，那個決定的理由就該寫在這一格 —— 否則下一個人遇到二選一時會隨手抓 Toggle。

**2 · `TLSettingsToggleRow` ↔ `TLSettingsRow` ＋ trailing 放 `TLSegmentedControl`**
這兩個在設定頁**同時存在**，長得也像（一列、右側一個控制項）。判準：開關列有專屬元件是為了保留 switch 的無障礙語意；其他就地控制走 `TLSettingsRow` 的 slot。這一格不寫，就會有人用 `TLSettingsRow` ＋ 自己塞一個 Toggle 進 slot，然後無障礙語意掉了。

**3 · `TLDivider` ↔ `TLDividedVStack`**
「誰負責畫線」的經典錯誤。判準：**永遠用 `TLDividedVStack`；直接用 `TLDivider` 是例外**，例外的情境要寫出來（如果沒有例外，那 `TLDivider` 就該標成 internal）。

第 3 組還有一個延伸問題值得你回答：**`TLDivider` 有沒有任何呼叫端直接用它？** 如果沒有，它不該是 public 元件，那會少一個讓人做錯的入口。

---

## Q4 · 三個無障礙缺口的優先度

### 〔決定〕`disabled` → `adjustable` → `selected`，理由是傷害的性質不同

**第一 · `TLSettingsToggleRow` 的 disabled**

它不是「不好用」，是**會說謊**。通知授權被拒之後開關仍然顯示開著，使用者以為提醒會響 —— 然後訓練完休息結束，什麼都沒發生。

它排第一的兩個理由：**影響所有使用者**（不只 VoiceOver），而且**錯誤的資訊比缺少的功能傷害大**。其他兩個是「做不到」，這個是「以為做到了」。

**第二 · `TLValuePicker` 的 adjustable trait**

VoiceOver 使用者完全無法調整滾輪，是功能封鎖，比排序問題嚴重。

但排第二而不是第一，因為**級距設定頁有「自訂」輸入框那一列** —— 有替代路徑，難用但不是死路。如果哪天滾輪出現在沒有替代路徑的畫面，它立刻升到第一。

**第三 · `TLListRow` 的 selected trait**

念不出選中狀態，使用者要靠試誤（點進去、聽、退回來）。可用但很差。

排第三的理由是它**同時是一個尚未決定的設計問題**（`isSelected` prop／新元件／維持現狀），而你已經決定等階段 2 的四個樣本再判斷。無障礙的修法會跟著那個決定走，現在單獨修可能白做。

**一句話的排序原則**：**會說謊的 > 做不到的 > 難用的。**

---

## 〔建議〕一條能擋跨語言不一致的檢查

你在 `CHANGELOG` 裡把 `--icon-m` 記成「檢查 16 擋不到、只能靠人眼」的實例，並接受那個限制。我認為**有一條便宜的做法**，而且它剛好會抓到這次這個 bug。

不要直接比對 Swift 與 CSS（那確實不值得做）。**用規格當中介**：

```
規格第 5 節（度量）與第 6 節（配色）已經列出這個元件用到的 token 名。
→ 檢查：CSS 鏡像那一段用到的 var(--…) 都在該元件規格第 5／6 節列出的 token 之內。
```

這次的情況：`TLBadge` 的規格第 5 節寫的是 `rowTitle`（或 `icon.inline`），CSS 用了 `--icon-m` —— **不在清單裡，會被擋。**

它抓不到「Swift 沒照規格寫」（那要 parse Swift 的樣式呼叫，成本高），但能抓到「CSS 鏡像沒照規格寫」，而 CSS 鏡像是手寫的、最容易飄的那一份。成本大概是一個字串集合的比對。

叫它檢查 17。
