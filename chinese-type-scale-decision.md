# 中文字級 — 第一批拍板

> 回覆 `scale-questions.md` 問題 1–4。第二批（display 尺度、膠囊三分法）另回。
> 日期：2026-08-31

每項標了性質：**〔決定〕**你問我拍板、**〔要改〕**有問題、**〔建議〕**可不採納。

---

## 先立一條規則，因為它決定四題裡的三題

四題都是「差 0.5–1px 要不要併」，所以先講判準，不然每題都會變成憑感覺。

**判準是「未來會不會分開走」，不是「現在差幾 px」。**

- 兩個角色**同值、且沒有任何理由會分開變動** → 一個 token（`textBody` 併進 `textSecondary` 就是這個情況）
- 兩個角色**同值、但各自有獨立的未來** → 兩個 token，值可以一樣

第二種不是浪費。它的價值是：日後「按鈕要大一點」這個決定，改的是一個 token，不是稽核 15 個使用處。**同值的兩個 token 不會製造漂移，字面值才會。**

我上一輪對 `textBody` 說「同值就是一個 token」，這裡的結論不同，差別就在上面那一句 —— `textBody` 與 `textSecondary` 是同一件事的兩個名字，按鈕與列標題不是。

---

## Q1 · `15.5`（15 處）→ **值改 15，但獨立成 `buttonLabel`** 〔決定〕

**0.5px 沒有立場留下。** 它不是一個尺度階，而且沒有任何設計文件指定過它。我的推測是：當初想讓按鈕文字比列標題「稍微重一點」，但調的是尺寸，而那件事該由**字重**做 —— 尺寸差 0.5px 在螢幕上不存在，字重差一階看得出來。

**但按鈕該有自己的角色名。** 按鈕與列標題有獨立的未來：按鈕受 44pt 觸控區約束、列標題受 56pt 列高約束，兩者日後很可能分開調。所以：

```
rowTitle     15   列標題
buttonLabel  15   按鈕與工具列文字（primary／secondary／destructive／取消儲存）
```

15 處全部指到 `buttonLabel`。視覺位移 −0.5px，肉眼無感。

**〔要改〕順帶一個衝突**：`15.5` 的使用處包含「sheet 標題」，而 Q4 的 `30` 也是「sheet 的大標」。所以現在有兩種東西都叫 sheet 標題 —— 一種是導覽列式的小標題（15.5），一種是內容區的大標（30）。這兩個要用不同的名字，否則下一個人會挑錯。建議前者就是 `buttonLabel` 所在的那一族（導覽列文字），後者見 Q4。

---

## Q2 · `13`（6 處）→ **不要當一個值決定，它是三個角色** 〔要改〕

6 處的用途是三種：小按鈕、編輯頁說明文字、歷史詳情的單位。把它們併成一個 token 會把三個不同的東西鎖在一起。

| 用途 | 處置 |
|---|---|
| 小按鈕 | 新增 `buttonLabelSmall: 13`，作為 `buttonLabel` 的配對值（照 `icon.paired` 的模式：屬於小按鈕變體，不是獨立尺度階） |
| 編輯頁說明文字 | 併進 `emptyHint`(12.5)，位移 −0.5px。**並把那個角色改名**，見下 |
| 歷史詳情的單位 | **留到第二批。** 單位是跟著數字走的（「60 **kg**」），它的尺寸由旁邊那個 display 數字決定，不由中文字級決定 |

**〔建議〕`emptyHint` 改名 `caption`。** 它現在的 4 處是空狀態提示，加上編輯頁說明文字之後就不只是空狀態了。名字綁死在一個情境會讓人不敢用它，然後又寫一個 13。

---

## Q3 · `20`（3 處）→ **併進 `cardTitle`(21)** 〔決定〕

對話框標題與訓練首頁的區塊標題，職責跟 `cardTitle` 一樣：**一組內容上方的標題**。1px 併掉，沒有獨立的未來。

**〔建議〕** 併完之後這個角色管三種情境（卡片、對話框、區塊），名字建議改 `blockTitle`。這條可以不做——比 Q2 的改名沒那麼必要，因為 `cardTitle` 不至於讓人不敢用。

---

## Q4 · `30` 獨立成 `sheetTitle`；`26` 需要你回一句話 〔決定 ＋ 一個反問〕

**`30`（2 處）獨立。** sheet 的大標比 page 的大標小，這是對的、而且是刻意的 —— sheet 是次級表面，標題跟著降一階。這不是漂移，是既有設計意圖，只是沒有名字：

```
pageTitle   34   頁面主標
sheetTitle  30   sheet 的大標（完成摘要、預覽）
```

**`26`（1 處，歷史詳情）—— 保留，並給它一個名字 `detailTitle`。**

已確認：它是 `detail.summary.name`（使用者取的訓練名，未取名 fallback「自由訓練」），NavigationLink push 的頁。且這張卡裡**沒有** `cardTitle` 可作對照，那一頁也**沒有** `pageTitle` —— 導覽列是系統畫的 inline 日期。

所以它不是「卡片内被放大的字」，是**頁標題住在卡裡**：導覽列只承載日期，真正的標題在内容區第一張卡。那是一個沒有名字的角色，不是漂移。

`34 / 30 / 26` 是連貫的下降：頁標題、sheet 大標、卡承載的頁標題。

**跟 `icon.l`(20) 的差別**（否則判準會前後不一）：`icon.l` 是**沒有職責的剩餘值**，找不到它在描述什麼；這個 26 描述得出來，而且會再出現（任何「導覽列只有日期／系統標題、真正的標題在內容區」的詳情頁都是它）。**一處使用撐不起一個階，但一個說得出來的職責撐得起。**

名字選 `detailTitle` 而不是 `summaryTitle`：它描述的是**導覽情境**（push 進來、導覽列不承載標題），不是某一張卡。

**不併進 `cardTitle`(21) 的理由**：那一頁最大的中文字會變 21，而旁邊的統計數字是 display 28 —— **標題比它的内容小 7px**。現在層級沒有反，併了才反。

**26 對 28 的張力留第二批**：兩個值差 2px 卻是兩種東西在爭同一張卡的最大字。那要跟 display 尺度一起判断，因為 28 有可能會動。

---

## 收斂後的中文字級

```
34    pageTitle       頁面主標
30    sheetTitle      sheet 大標                      ← 新
26    detailTitle     導覽列不承載標題時，內容區的頁標題   ← 新
21    cardTitle       卡片／對話框／區塊標題            ← 併入 20
16    emptyTitle      空狀態標題
15    rowTitle        列標題
15    buttonLabel     按鈕與導覽列文字                 ← 新，取代 15.5
14    rowValue        列的值
13    buttonLabelSmall 小按鈕（buttonLabel 的配對值）    ← 新
12.5  caption         說明／提示文字                   ← 原 emptyHint，併入 13 的說明文字
12    badgeText       徽章
12    segCompact      緊湊分段
11.5  rowSub          列副標
10.5  kicker          小標示
```

**17 個值 → 13 個角色（其中兩對同值）。**

---

## 三件不在你四題裡、但這次要一起處理的

### 〔要改〕`rowIcon: 17` 不屬於中文字級

它在「已命名的角色」清單裡，但它是 SF Symbol 的點數，不是中文字級。而我們上一輪剛把 icon 收斂成 `inline: 14` / `standalone: 22` —— **17 這個值在新的 icon 尺度裡沒有位置。**

要嘛它其實是 `icon.paired` 的一員（列左側圖示，配對 56pt 列高），要嘛它就該是 `icon.inline`(14)。請一併查，別讓它留在字級群組裡躲過 icon 那次的收斂。

### 〔決定〕零星值的歸屬

四題沒涵蓋的那些，照上面的角色分派即可，不需要再問我：

- `15` × 4（對話框主鈕、訓練中列）→ 主鈕 `buttonLabel`、列 `rowTitle`
- `14` × 3（文字按鈕、歷史詳情的動作名）→ 文字按鈕 `buttonLabel`（−1px）、動作名 `rowValue`
- `19` × 1（月曆標題）→ `cardTitle`(21) 或 `emptyTitle`(16)，看它的視覺層級；一處撐不起新階
- `13.5` × 1、`12` × 1、`10.5` × 1 → 就近併進 `caption`／`badgeText`／`kicker`

### 〔要改〕`10` 與 `9.5` 不要直接 token 化

這兩個值（各 1 處）**低於可讀下限**，而 `kicker`(10.5) 本身已經有問題——我第四輪指出它用 `textTertiary` 對比只有 2.6:1，是整包最難讀的文字。

10 與 9.5 不該併進 kicker，**應該連同 kicker 一起檢視**：那三處到底要不要這麼小？中文在 10px 以下筆畫會糊，而且語言 ×1.08 之後是 10.8／10.26，仍然太小。

建議這三處各自升到 `kicker`(10.5) 以上，並把 kicker 的色改 `textSecondary`。這是設計改動不是收斂，記進已知缺口。

---

## 附：你提的「相鄰值最小差距」規則現在寫得出來了

你在附錄說要等尺度定了才寫得出來。有了上面的表，規則是：

> **同一個字級群組內，兩個不同的值差距不得小於 1。**
> 同值的兩個角色是允許的（它們表達不同的未來，不是不同的尺寸）。

這條會擋掉 15／15.5 並存、13／12.5 若都獨立、以及未來任何 0.5 的漂移。`12.5` 與 `11.5` 差 1，剛好合格 —— 這也說明 1 是對的門檻，再嚴會擋掉既有的合理階。
